import React from "react";
import {Button, Image, Stack, StackDivider, Text} from "@chakra-ui/react";

import Logo from "/assets/logo.svg";
const Hero = () => {
  return <>{/* <Image alt="logo" height={[32, 32]} src="assets/logo.svg" width={[32, 32]} /> */}</>;
};

export default Hero;
