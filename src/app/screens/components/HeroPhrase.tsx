import React from "react";
import {Button, Image, Stack, StackDivider, Text} from "@chakra-ui/react";

const HeroPhrase = () => {
  return (
    <>
      <Stack className="HeroPhrase" />
    </>
  );
};

export default HeroPhrase;
